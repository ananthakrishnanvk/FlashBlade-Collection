bugfixes:
 - purefb_server - Fix pydantic ``ValidationError`` when ``dns`` or ``directory_service`` is set for an existing server; the update path was passing the whole parameter list to ``Reference(name=...)`` instead of iterating per item.
 - purefb_server - Correct ``directory_service`` update path which wrote to ``Server.dns`` instead of ``Server.directory_services`` and could silently replace the server's DNS list.
